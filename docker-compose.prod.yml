# Production stack for a single Ubuntu host.
#
#   docker compose -f docker-compose.prod.yml up -d --build
#
# Postgres and Redis are NOT published to the host (only `expose`d on the
# internal compose network) — this matters because Docker-published ports
# bypass ufw. Only Caddy publishes 80/443.
#
# Variables read from ./.env (compose substitutes ${...} automatically):
#   DOMAIN             public hostname, e.g. clickup-sync.example.com
#   POSTGRES_PASSWORD  must match the password embedded in DATABASE_URL
#
# Memory tuning (small host): every service has a mem_limit so a runaway process
# is killed ALONE (container OOM) instead of triggering a global OOM that takes
# down other containers. Node caps the V8 heap so it GCs hard and throws before
# ballooning. Limits are ceilings, not reservations — steady usage sits below them.

services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    mem_limit: 512m
    environment:
      POSTGRES_DB: clickup_sync
      POSTGRES_USER: clickup
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U clickup -d clickup_sync"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:8-alpine
    restart: unless-stopped
    mem_limit: 320m
    # noeviction: BullMQ must NEVER have job keys evicted (that corrupts queues);
    # Redis rejects writes instead if it ever hits the ceiling.
    command: ["redis-server", "--appendonly", "yes", "--maxmemory", "256mb", "--maxmemory-policy", "noeviction"]
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    # CI publishes this image to GHCR; the deploy pulls it by commit SHA
    # (IMAGE_TAG), falling back to :latest for a manual `docker compose up`.
    # `build: .` is kept so you can still build locally with `up -d --build`.
    image: ghcr.io/rashedulhasansojib/clickup-sync:${IMAGE_TAG:-latest}
    build: .
    restart: unless-stopped
    # This single process runs web + workers + cron, including backfills, so it
    # gets the largest ceiling. The heap cap makes V8 GC hard and throw before it
    # balloons — a runaway backfill can otherwise OOM the whole host.
    mem_limit: 896m
    env_file: .env
    environment:
      NODE_OPTIONS: --max-old-space-size=576
    expose:
      - "3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    # Apply migrations, then start. prisma migrate deploy is idempotent.
    command: sh -c "npm run prisma:deploy && npm run start:prod"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    mem_limit: 96m
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN:?set DOMAIN in .env}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api

volumes:
  postgres_data:
  redis_data:
  caddy_data:
  caddy_config:
